version: 1.0.0
metadata:
  title: Project A
  description: Template of the project
  project: project_a
  workspace: default
orchestration:
  documents:
    - "documents.yaml"
infrastructure:
  models:
    - "models.yaml"
  backends:
    - "backends.yaml"
  defaults:
    chat_model: gemma3
    embedding_model: nomic-embed-text
