version: 1.0.0
metadata:
  title: Project A
  description: Template of the project
  project: project_a
  workspace: default
infrastructure:
  models:
    - "models.yaml"
  defaults:
    chat_model: gemma3
    embedding_model: nomic-embed-text
